获取当前天气、未来三天天气
1、使用的是和风天气API

获取位置问题：
1、react-native-geolocation-service，获取经纬度进行高德逆地理编码API调用，获取城市，虚拟机需要开启google play服务，需要有处理获取位置的服务程序app/services/location，以及
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> 安卓添加权限， 修改android/src/main/AndroidManifest.xml


图片上传问题：
1、react-native-image-picker， 3.1.4版本，minSdkVersion最少要21以上，使用的react-native 0.61.5，修改android/build.gradle
2、aliyun-oss-rn，需要更改aliyun-oss-rn npm包的android/build.gradle,提高SDK版本号，以及修改项目AndroidManifest.xml,添加android:allowBackup="true"

```
buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:1.3.1'
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    buildToolsVersion '28.0.3'

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
    }
    lintOptions {
        abortOnError false
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
    implementation 'com.aliyun.dpa:oss-android-sdk:+'
}
  
  
```


闹钟问题

1、react-native-alarm-clock（只适用于android）,目前yarn只能下载1.0.0，npm就可以下下载2.0.0，所以用法有些不一样，需要更改react-native-alarm-clock npm包的android/build.gradle,提高SDK版本号
```
apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    buildToolsVersion "28.0.3"
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'com.facebook.react:react-native:+'
}

```

选择下拉框问题

1、react-native-picker-select，报错信息 `"RNCAndroidDialogPicker" was not found in the UIManager`，参考Issues https://github.com/lawnstarter/react-native-picker-select/issues/402，
解决办法，降级@react-native-picker/picker 至 1.8.3


管理员CMD
#keytool -genkey -dname "CN=wushaobin,OU=个人,O=个人,L=深圳,ST=广东,C=CN" -alias readit -keyalg RSA -validity 400000 -keystore readit.keystore
#keytool -importkeystore -srckeystore readit.keystore -destkeystore readit.keystore -deststoretype pkcs12
#keytool -list -v -keystore readit.keystore -storepass Wu13539672487
或 keytool -genkeypair -v -keystore readit.keystore -alias readit -keyalg RSA -keysize 2048 -validity 400000